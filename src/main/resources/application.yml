server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    data-username: root
    data-password: root
    url:jdbc:mysql://192.168.50.108:3306/zhili_base?characterEncoding=utf8

  redis:
    url:
    database:
    host:
    jedis:
      pool:
        max-active:
        max-wait:
        max-idle:
        min-idle:
